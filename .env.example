# Perenual (perenual.com) plant care database API key.
# Get a free key at https://perenual.com/api-key (free tier: 100 requests/day).
# Used only when searching for a species and downloading its care info the first time —
# after that, the result is cached on-device and no more requests are made for that species.
EXPO_PUBLIC_PERENUAL_API_KEY=
